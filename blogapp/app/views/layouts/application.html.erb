<!DOCTYPE html>

<html>
<head>
  <title>Blogapp</title>
  
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>


    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application" 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <script src="//code.jquery.com/jquery-1.11.2.min.js"></script>

  </head>

  <body>
  

  <nav class="top-bar" data-topbar role="navigation" >
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">My Site</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if current_user %>
        <li style="color:white; margin-top: 9px">Signed in as <%= current_user.email %></li>
        <li><%= link_to "Sign Out", signout_path %></li>
      <% else %>
        <li><%= link_to "Sign In", new_user_session_path %></li>
      <% end %>
      <li class="active"><a href="#">Right Button Active</a></li>
      <li class="has-dropdown">
        <a href='#'>Post!</a>
        <ul class="dropdown">
          <li id='vid' value='/new-video-post'><%= link_to "new text post", new_video_post_path %></li>
          <li class="active"><a href="#">Active link in dropdown</a></li>
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
      <li><a href="#">Left Nav Button</a></li>
    </ul>
  </section>
</nav>
<% if current_user %>
  Welcome to SHINJI! You are signed in as <%= current_user.email %>.
  <%= link_to 'sign out', signout_path %>
<% else %>
  Please <%= link_to 'sign in', new_user_session_path %> first.
<% end %>

<div class="row">
    <%= yield %>
</div>


  </body>
</html>
